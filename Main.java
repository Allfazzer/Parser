import java.util.List;

public class Main { 
    public static void main(String[] args) {
        // Initialize a Scanner
        List tokens = new Scanner().getTokens();

        // Initialize Parser
        Parser parser = new Parser();
    }
}